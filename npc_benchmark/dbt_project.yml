name: 'npc_benchmark'
version: '1.0.0'
config-version: 2

profile: 'npc_benchmark'

model-paths: ["models"]
target-path: "target"
log-path: "logs"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  silver_path: "../data/silver"

models:
  npc_benchmark:
    staging:
      +materialized: view
    marts:
      +materialized: table
