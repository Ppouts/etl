# Projet npc_brain

## Setup du projet

```bash
python -m venv .venv

# source .venv/bin/activate

# .venv/Scripts/activate

pip install dotenv numpy openai
```

- Extension `Python` sur VSCode
- Extension `Jupyter` sur VSCode